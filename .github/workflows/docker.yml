# Workflow (update build) to run whenever we update main
name: "Update Docker Build"
on:
  push:
    branches:
      - main
      - add/docker-ci
  workflow_dispatch:

jobs:
  build:
    name: "Build"
    runs-on: "ubuntu-latest"

    permissions:
      packages: write
      contents: read

    steps:
      - name: "Set up JDK 17"
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: "Checkout repository"
        uses: actions/checkout@v4

      - name: "Run Gradle installDist"
        run: gradle installDist

      - name: "Debug Gradle output dynamically"
        run: |
          echo "Checking Gradle output dynamically..."
          ls -R build/install
          output_dir=$(ls build/install)
          echo "Found output directory: $output_dir"
          ls -la build/install/$output_dir/bin

      - name: "Configure Docker metadata using Docker/metadata-action"
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/Yellowjeebz/PRP-Recipe-Suggestion-Solution
          tags: |
            type=sha
            type=raw,value=latest

      - name: "Authenticate with Docker"
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "Set up Docker buildx"
        uses: docker/setup-buildx-action@v3

      - name: "Build and upload the Docker image"
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
